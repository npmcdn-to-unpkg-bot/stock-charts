"use strict";


var ScaleUtils = {
	flattenData(data, xAccessors, yAccessors) {
		var xValues = [];
		var yValues = [];

		data.forEach( (row) => {
			xAccessors.forEach( (xAccessor) => {
				var x = xAccessor
			});
		});
	}
}
